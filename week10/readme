return 
返回0代表正常
返回1-126代表异常
127代表
128代表
129代表

三类返回类型
exit
_exit
return
前两个是操作提供的
return是glibc用户提供的
printf是行缓冲，是根据\n来判断换行的
puts是全缓冲，全缓冲的冲刷是以缓冲区的

vfork 是当年对fork的改进，
子在父先亡——孤儿进程
return会释放掉局部变量所占用的资源，从而导致段错误；

